import { Button } from "@chakra-ui/react";

import IColor from "../types/icolor";

const AButton = ({ color, tone }: IColor) => {
  return (
    <Button>
      Click here
    </Button>
  );
};

export default AButton;
